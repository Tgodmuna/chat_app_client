import type React from "react";
type stateTypes = {
  username: string;
  email: string;
  password: string;
  name: string;
  phone: string;
  location: string;
  gender: string;
  age: string;
  status: string;
  role: string;
};

export function handleSubmit ( e, formData, statesetter, state ) {
    
}